nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"
    tag "PIPELINE"

    test("Default test for reproducibility") {

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }

    }

}
